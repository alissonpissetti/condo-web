export const environment = {
  production: true,
  /** Ajuste para o URL público da condo-api em produção */
  apiUrl: 'https://api.meucondominio.cloud',
  /**
   * Na API: FRONTEND_PUBLIC_URL deve apontar ao URL deste site (ex.: https://app.meucondominio.cloud)
   * para links de convites e da página pública `/comunicado/publico?token=…`.
   */
};
